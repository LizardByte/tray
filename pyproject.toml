[project]
name = "tray"
version = "0.0.0"
description = "A cross-platform system tray library"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "clang-format==21.*",
    "gcovr==8.6",
]
